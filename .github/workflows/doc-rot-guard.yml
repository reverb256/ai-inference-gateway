name: Doc Rot Guard
on:
  schedule:
    - cron: '0 0 * * *'
  issues:
    types: [labeled]
jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Flag stale docs
        run: echo 'Kagent enrichment triggered'